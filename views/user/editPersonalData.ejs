<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Uredi Lične Podatke</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/stylesheets/userForm.css">
</head>
<body>
<div class="container mt-5">
    <h1 class="text-center mb-4">Uredi lične podatke</h1>
    <form action="/user/profile/personal_data?_method=PUT" method="POST" class="border p-4 rounded shadow">
        <div class="mb-3">
            <label for="phone_number" class="form-label">Broj telefona</label>
            <input type="text" id="phone_number" name="phone_number" value="<%= personalData.phone_number || '' %>" class="form-control">
        </div>
        <div class="mb-3">
            <label for="address" class="form-label">Adresa</label>
            <input type="text" id="address" name="address" value="<%= personalData.address || '' %>" class="form-control">
        </div>
        <div class="mb-3">
            <label for="date_of_birth" class="form-label">Datum rođenja</label>
            <input type="date" id="date_of_birth" name="date_of_birth" value="<%= personalData.date_of_birth ? personalData.date_of_birth.toISOString().substring(0, 10) : '' %>" class="form-control">
        </div>
        <div class="mb-3">
            <label for="about_me" class="form-label">O meni</label>
            <textarea id="about_me" name="about_me" class="form-control" rows="4"><%= personalData.about_me || '' %></textarea>
        </div>
        <button type="submit" class="btn btn-primary">Spremi promjene</button>
    </form>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
