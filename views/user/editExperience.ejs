<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Uredi Radno Iskustvo</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/stylesheets/userForm.css">
</head>
<body>
<div class="container mt-5">
    <h1 class="text-center mb-4">Uredi radno iskustvo</h1>
    <form action="/user/profile/work_experience/<%= experience.id %>?_method=PUT" method="POST" class="border p-4 rounded shadow">
        <div class="mb-3">
            <label for="company_name" class="form-label">Kompanija</label>
            <input type="text" id="company_name" name="company_name" value="<%= experience.company_name %>" class="form-control" required>
        </div>
        <div class="mb-3">
            <label for="position" class="form-label">Pozicija</label>
            <input type="text" id="position" name="position" value="<%= experience.position %>" class="form-control" required>
        </div>
        <div class="mb-3">
            <label for="start_date" class="form-label">Datum početka</label>
            <input type="date" id="start_date" name="start_date" value="<%= experience.start_date.toISOString().substring(0, 10) %>" class="form-control" required>
        </div>
        <div class="mb-3">
            <label for="end_date" class="form-label">Datum završetka</label>
            <input type="date" id="end_date" name="end_date" value="<%= experience.end_date ? experience.end_date.toISOString().substring(0, 10) : '' %>" class="form-control">
        </div>
        <div class="mb-3">
            <label for="description" class="form-label">Opis</label>
            <textarea id="description" name="description" class="form-control" rows="4" required><%= experience.description %></textarea>
        </div>
        <button type="submit" class="btn btn-primary">Spremi promjene</button>
    </form>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
