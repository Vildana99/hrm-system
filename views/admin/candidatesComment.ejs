<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ocjena i Komentar</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/stylesheets/userForm.css">
</head>
<body>
<div class="container mt-5 d-flex justify-content-center">
    <div class="card shadow-lg" style="width: 100%; max-width: 600px;">
        <div class="card-body">
            <h2 class="text-center mb-4">Dodaj ocjenu i komentar</h2>
            <form action="/admin/candidates/jobs/<%= jobId %>/applications/<%= applicationId %>/review" method="POST">
                <!-- Ocjena -->
                <div class="mb-3">
                    <label for="score" class="form-label">Ocjena (1 - 5):</label>
                    <select class="form-select" id="score" name="score" required>
                        <option value="" disabled selected>Odaberi ocjenu</option>
                        <option value="1">1</option>
                        <option value="2">2</option>
                        <option value="3">3</option>
                        <option value="4">4</option>
                        <option value="5">5</option>
                    </select>
                </div>

                <!-- Komentar -->
                <div class="mb-3">
                    <label for="comments" class="form-label">Komentar:</label>
                    <textarea class="form-control" id="comments" name="comments" rows="4" placeholder="Dodajte vaš komentar" required></textarea>
                </div>

                <button type="submit" class="btn btn-primary w-100">Dodaj recenziju</button>
            </form>
        </div>
    </div>
</div>

</body>
</html>

